# Open mv 开发工作记录
硬件：
主控：OpenMV Pro Plus (STM32H743)
摄像头：OV5640
分辨率：详见页面https://book.openmv.cc/image/sensor.html
固件：4.5.9版本

——————————————————————————————————————————————————————

Phase 0：环境与图像采集
*目标：获得稳定、低曝光、灯条清晰的图像
根据现场光照调节曝光时间（建议起始 1~5 ms）和增益（0~10）。
在 IDE 中观察灯条是否高亮且背景较暗。
*验证标准：肉眼可见灯条清晰，背景干扰少。

——————————————————————————————————————————————————————

Phase 1：颜色差分与灯条提取
*目标：提取出图像中的红/蓝灯条候选区域。
#方法一：LAB 颜色空间 find_blobs——利用 OpenMV 内置的 find_blobs 和 LAB 阈值，可同时提取红蓝灯条
#方法二：通道差分法——对 RGB565 图像直接计算红蓝差分，再二值化
灯条形态学筛选：对每个 blob 应用相应的过滤条件
*验证标准：典型场景下能提取全部灯条，无明显误检（如地面反光、其他灯光）。

——————————————————————————————————————————————————————

Phase 2：灯条几何配对
*目标：将属于同一装甲板的左右两根灯条配对。
需要编写配对算法以及遍历组合

———————————————————————————————————————————————————————

Phase 3：建立一个 geometry_score
逐渐形成一个评分：
eg：geometry_score = (
    height_score * 0.25 +
    angle_score  * 0.25 +
    y_score      * 0.20 +
    distance_score * 0.30
)
设MIN_GEOMETRY_SCORE = 0.55；只要geometry_score > 0.55就送CNN

————————————————————————————————————————————————————————

Phase 4：ROI 提取与校正
*目标：从灯条对中裁剪出中间图案区域，归一化到固定尺寸。
#方法：旋转校正 + 裁剪
由于灯条可能倾斜，建议先根据灯条角度进行旋转校正。如果角度差很小（<5°），可跳过旋转直接裁剪。
#步骤：
#1.计算平均角度（若支持 rotation_deg 或通过最小外接矩形拟合）。
#2.对整幅图像旋转 -avg_angle（使用 img.rotation_corr()，注意它会返回新图像）。
#3.在旋转后的图像中重新提取灯条对（或直接用旋转前的 blob 坐标近似）。
#4.剪两灯条之间的区域（左右灯条内边界之间，垂直范围取两灯条的整体包围框）。
#5.缩放到固定尺寸（如 48×48）并转为灰度。

———————————————————————————————————————————————————————

Phase 4：中间图案识别
#方案 A：模板匹配（快速启动）
准备模板：
采集基地图案及常见数字（1~9 或所需类别）的样本，缩放至目标尺寸（如 48×48），保存为 .pgm 文件。在 OpenMV 中加载模板。需要编写匹配函数。

#方案 B：Tiny CNN（Edge Impulse 训练）
数据采集：使用 Phase 3 的方法从现场视频中截取大量 ROI 样本，分为“基地”和“非基地”（或包含具体数字类别）。
数据标注：在 Edge Impulse Studio 上传图像并标注标签，建议每类至少 200~500 张。

模型设计：
输入：48×48 灰度图（或 32×32）
网络：MobileNetV1 alpha 0.1 或自定义轻量网络（2~3 个卷积层）
输出：两类（二分类）或多个类（如果区分数字）
训练与验证：目标验证准确率 > 95%。
部署：
在 Edge Impulse Deployment 页面选择 OpenMV，下载 .tflite 模型。
在 OpenMV 中加载并推理：

———————————————————————————————————————————————————————

Phase 5：融合与输出
综合评分：
1.融合颜色置信度、几何合理性、分类置信度
2.确认数据结构：
armor_info = {
    'color': 'red',          # 'red' 或 'blue'
    'type': 'base',          # 'base' 或其他类型
    'center': (cx, cy),      # 图像坐标
    'score': 0.85,           # 综合置信度
    'roi': (x, y, w, h)      # 装甲板矩形
}
3.连续帧确认：维护一个目标列表，记录每个候选目标出现的帧数和位置，连续 3 帧检测到且分类一致才输出，降低误检。
验证标准：实时视频流中能稳定框选目标并输出正确信息，帧率满足要求（>20 FPS）。

———————————————————————————————————————————————————————

Phase 6：现场标定与优化
参数集中管理：
将所有阈值和配置集中在代码顶部，方便现场修改

性能优化建议：
限制 find_blobs 返回的最大 blob 数：
仅在检测到灯条时才进行 ROI 裁剪和分类。
使用 sensor.set_windowing() 缩小搜索区域（如果目标大致位置已知）。
尽量复用图像缓冲区，避免不必要的拷贝。
如果使用 CNN，考虑降低输入分辨率到 32×32。

标定流程
在现场布置典型场景，运行程序。
使用 OpenMV IDE 的“阈值编辑器”辅助调整颜色阈值。
逐项检查 Phase 1~5 的输出，调整几何参数。
记录不同光照条件下的参数变化，必要时使用动态曝光或自动调整。

————————————————————————————————————————————————————————

10/20前目标：完成无人机的openmv识别装甲板

##9/10开发
1.部署Openmv IDE以及Openmv vscode插件
2.成功实现Open mv摄像头的调用
遇到的待解决的问题：
1.Openmv固件版本待升级至5.0.0 （需要使用DFU升级但是未在板子上找到reset短接口）
2.Openmv的注册主板许可证的验证 联系商家暂未回复

#9/11开发
1.让摄像头成功跑通色块识别 但是效果极差 等待yolo模型
2.使用IDE中的色彩阈值编辑器标定摄像头中的色彩阈值

思考：研究发现在openmv上部署yolo可能性较低，另一开源项目https://github.com/SZURPVision/26_NNDeployment_Lib_and_Detection_Models适配性也较差，是Linux+C++框架，打算尝试openmv唯一支持的神经网络Edge Impulse

#9/12开发
确认开发方向
md文件编写前面的计划表

#9/13开发
## 今日开发内容
1. 完成 OpenMV 装甲板识别程序的整体流程搭建，包括红色灯条提取、候选灯条筛选、灯条配对和装甲板目标输出。
2. 基于 LAB 颜色阈值进行红色区域识别，并通过像素数量、灯条长度和 elongation 等条件对候选灯条进行初步筛选。
3. 完成双灯条几何配对算法，通过灯条角度差、长度差、平行方向偏移和法线方向间距等参数判断两根灯条是否能够组成装甲板，并加入综合评分机制选择最优目标。
4. 针对远距离目标像素较少的问题，对不同距离下的几何评分权重进行了动态调整，提高远距离小目标的识别容错能力。
5. 优化最终目标的显示方式，去除装甲板外接框和灯条连接线，只保留装甲板中心绿色十字标记，减少不必要的图像绘制操作。
6. 保留原有 Debug 功能，可分别控制原始 blob、候选灯条、最终目标以及串口调试信息，方便后续识别参数调试。
7. 对 FPS 输出方式进行了调整，由原来的每帧打印改为每 30 秒输出一次，减少频繁串口打印对程序运行效率的影响。
8. 完成红蓝识别并且能够在实验室环境下准确标注
## 当前结果
目前已能够完成红/蓝色灯条检测、灯条配对以及装甲板中心位置识别，程序可以输出最终装甲板中心十字。
当前 OpenMV 实际运行帧率约为 25 FPS，整体识别逻辑已经基本完成，但实时性能仍有进一步优化空间。


#9/15开发
1.解决Red颜色识别中心抖动的问题
2.解决debug调试系统控制按键的更新 持续显示画面fps和颜色识别（五秒一次）